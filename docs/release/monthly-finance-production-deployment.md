# 月度财务正式上线记录（2026-09-06）

**已上线并完成验收。** 沿用原正式网站 https://admin-salary.lovemiemie.com 和 API https://api-salary.lovemiemie.com 。没有新建站点、仓库或数据库；计划器及定时自动执行均保持 false。

## 已部署版本

- 应用提交：`ea2b7f6a162f92dc228c1d22448f738381a0e787`。
- 正式标签：`monthly-ea2b7f6a162f`。
- 批准源码包 SHA256：`a867d3a2607eb7dd83ebb727e16cade423f9081b63a777811578d0e64c0622cd`。
- API：`sha256:8de5423b041f59f55ffbed072bb1712d2f5e13c218993f17528fd8bd18b98a81`。
- Web：`sha256:b52b44407ba9d16d5cab6e4ef82dbedf6a53fec9271a1bad450b395733f98daf`。

生产从批准包的 469 个文件构建，构建前后逐文件 SHA256 验证通过，27 个迁移校验值匹配。这里记录的是生产实际构建镜像，和本地审核镜像 ID 不同；不是静默替换应用代码。此后的提交只保存发布脚本和证据，不改变已部署的应用提交。

## 执行时间与门禁

以下为北京时间（UTC+8），原始 UTC 阶段记录见同目录 `monthly-finance-production-evidence.json`。

| 阶段 | 时间/结果 |
|---|---|
| 最终执行开始 | 2026-09-06 19:36:55 |
| 新鲜备份完成 | 19:37:13–14，校验和与认证解密、gzip 完整性均通过 |
| 停止 API/Web 写入 | 19:37:14 |
| 执行迁移 | 19:37:15，8 项全部成功，最终 27 项完整且校验值匹配 |
| 新版健康与正式 HTTPS 登录通过 | 19:37:24 |
| 一次真实整月刷新验收完成 | 19:40:58 |
| 正式 Edge 页面验收完成 | 19:47:26 |

最终停写到健康/登录通过约 10 秒（按阶段记录估计，不是逐请求可用性测量）。上线前没有 pending/running/retry_wait 任务，没有其他活动数据库事务；停写后再次检查在途任务及人工财务哈希。没有解锁月份、填写猜测 SUB/费率或插入测试账目。

备份实际文件：`/opt/salary-settlement-admin/backups/postgres-full-20260906T113713Z.sql.gz.enc`，545371 bytes，SHA256 `72f480b1b6ce41116f5ebe095d19e5fd7c1ab6055bb0c8acf4213300e873efe5`。既有备份健康脚本验证实际文件、sidecar 校验值和认证解密/gzip 完整性；本轮没有做生产整库恢复。

## 验收结果

- 正式 HTTPS 管理员登录成功，未登录访问大盘返回 401；没有通过虚构低权限账号宣称完整角色矩阵回归。
- 正式页面五个核心列、ROI、月份切换、刷新入口通过；Edge 1440×1000 下显示 6 个员工行，明细 20 条，页面脚本错误 0。HTTP 明细第 1/2 页各 20 条，无交叉重复。
- 对未锁的 2026-08 只触发一次统一刷新。四个联盟（脱敏 A01–A04）、Airwallex、PhotonPay 全部 completed，完整覆盖至 `2026-08-31T16:00:00Z`，即 GMT+8 的 9 月 1 日零点。未再次重复整月拉取。
- 当月卡支出从 147 条补全至 5360 条，原有 147 条的金额、归属、状态及标识保持一致，重复外部交易号为 0。
- 3 条既有人工收入/调整记录哈希保持一致；人工费用和月卡费率同样保持原值（本来为空）。CAKE 按原生佣金 + 已确认人工调整处理。
- 刷新后无在途同步，两个自动调度开关仍 false。

## 过程中处理的问题及回退情况

第一次准备退出是挂载到应用目录外的辅助脚本找不到 Prisma 模块；补充 `NODE_PATH=/app/node_modules` 后在本地旧版镜像验证并继续，没有改应用候选。

随后一次尝试在 19:19:31 停写后调用既有备份服务失败，19:19:33 已启动恢复旧镜像，并随后确认就绪。这次没有执行迁移。最终沿用既有服务可运行时先备份、再停写的顺序，并增加停写后的财务哈希门禁，成功完成上线。

**没有迁移失败，没有上线后镜像回退，没有数据库回退。** 保留旧 API/Web 精确镜像及新增 schema 的应用回退方案；不得为了回退盲删新表、触发器或恢复整库。

实际操作脚本保存在 `deploy/monthly-finance/`，用于审计此次执行，不是可直接重复运行的通用发布器：固定绑定本次批准包、原始基线及正式路径。其默认执行需要 root 和交互登录；当前生产已是新版，旧基线门禁会拒绝重复运行。早期 `scripts/monthly-finance-production-release.sh` 仅是审核准备稿，不是本次实际执行脚本。

## 剩余业务配置

仍有 2 条员工展示行待设置统一 SUB，Airwallex/PhotonPay/Adpos 三类真实月费率未填写，因此页面如实显示“数据尚不完整”和“待填写手续费”，ROI 暂不显示数值。这些是业务配置待办，不是刷新失败。独立 Portal 月报对账仍未取得，不能把本次 API 全月验收宣称为独立财务审计。

脱敏机器证据：[monthly-finance-production-evidence.json](monthly-finance-production-evidence.json)。页面截图留在本地忽略目录 `tmp/monthly-finance-deployment/`；不将真实员工数据截图、登录密码或会话密钥提交到仓库。
