# CAKE 月度核对正式上线记录

2026-09-06 已按用户“上线”授权部署至原站点 https://admin-salary.lovemiemie.com ，应用提交 `ea3b9f31e398a0aa4ea29413f3e42fedd731973a`，标签 `cake-review-ea3b9f31e398`。

北京时间 22:38:26 停止旧写服务，22:38:29 开始唯一新增迁移，22:38:43 正式 HTTPS 健康、真实登录和 API 验收通过，22:38:45 完成全部部署门禁。维护窗口约 17 秒；22:43:02 正式 Edge 页面验收通过。没有发生回退。

实际生产镜像：

- API：`sha256:d789b35b384f0dcbd2b7acf1d7a4ee7d6977c8717fadcb62728263da432d4775`
- Web：`sha256:edbcacbb478ea54d68616a60b7ebece96f437ebc6007d95d2f0c98d5a375a9cd`

源码包 SHA256 `f1cd08271416929a0abdc08ef2875534beaae4cdccbd89b968d4d249319df466`，构建前后与归档逐文件比较一致；沿用已验证 Node 22 Dockerfile 和正式 API 地址。

## 迁移、备份和保留结果

现有 27 项迁移校验和匹配后，只新增 `20260907010000_cake_monthly_income_review`，合计 28 项。仅创建核对证据表及账号月份约束、锁月触发器，无历史收入回填。生产验收结束证据表仍为 0 条，没有替用户确认、取消或合成核对数据。

旧 API 运行期间完成新鲜备份 `postgres-full-20260906T143823Z.sql.gz.enc`，SHA256 `804ca539427ac6061f7e49009180e8ced6d3d343c4ca71ea3acb0dbb4cf11951`。实际文件校验和、认证解密及 gzip 完整性检查全部通过。

发布前后全部收入、人工调整、手工成本、卡消费、月费率、映射、员工统一 SUB、锁月、别名、排除配置及同步任务摘要一致。七、八、九月 API 金额/费率/逐行账目摘要相同。七月 PhotonPay 保持成功 5,807、失败 0、整月覆盖完成；没有重新触发同步。两个自动开关仍关闭，活动同步任务为 0。

## 正式页面验收

- 七月 Blitzads：已有已确认调整（3 条）；此依据仅证明实际 SUB 调整，不能推断账号每一笔均已核对。BA2：未核对GMT+8时区佣金。
- 八月、九月 Blitzads 与 BA2：均显示未核对GMT+8时区佣金，并可见“已核对，无需调整”入口。
- 三个月的账号级“查看 / 调整”跳转均保留正确账号和月份；五个核心列正常，0 页面错误。
- 正式验证只读，没有点击确认、取消、创建调整或刷新。确认/取消/指纹变化/权限和锁月行为采用已通过的 30 项 CAKE、15 项大盘及隔离浏览器/迁移证据，未虚称生产写入测试。
- 未登录访问返回 401；正式验收结束仅注销本次短期会话，并确认该会话 `/me` 返回 401。

回退基线为限流热修 `monthly-hotfix-4328cada3fe0`，API `a0b62028409b94c22cfdabfad3bc33ff2639030a7aebfd25aada2b8d96dfde0e`、Web `7361ddee07ca26020b8bbeb878dc494d97d5ca0b7c6fd3509891efe3658c6ca1`。如需应用回退，保留新增表和全部数据，不回退到更早版本覆盖限流修复，也不删表或整库恢复。

机器证据：`cake-monthly-review-production-evidence.json`。本地原始证据及截图：`tmp/cake-monthly-review-deployment/`。Influx 未映射、Blitzads 缺 SUB 以及待统一员工标识仍按真实业务状态保留，未猜填。
