# 月度大盘限流热修生产交付

已上线应用提交：`4328cada3fe053159d08be7394625ef462117903`；标签：`monthly-hotfix-4328cada3fe0`。正式站点仍为 https://admin-salary.lovemiemie.com 。新增 CAKE 人工核对候选 `ea3b9f3` 尚未上线，不能与此次热修混称。

## 发布时间与验证

2026-09-06 北京时间 21:42:46 停止旧写服务，21:42:56 新版通过正式 HTTPS 健康和真实管理员登录；切换窗口约 10 秒。21:44:15 完成单源验收与保留检查，21:54:24 正式 Edge 页面验收通过，7 行、五个核心列、月份切换、分页 20 条、0 页面错误。验收会话已单独注销并验证 `/me` 返回 401。

上线前从固定源码包完成 Node 22 API/Web 构建、95 项测试及三个桌面宽度的隔离浏览器验收。实际生产镜像：

- API：`sha256:a0b62028409b94c22cfdabfad3bc33ff2639030a7aebfd25aada2b8d96dfde0e`
- Web：`sha256:7361ddee07ca26020b8bbeb878dc494d97d5ca0b7c6fd3509891efe3658c6ca1`

备份在旧 API 仍运行时完成，文件 `postgres-full-20260906T134243Z.sql.gz.enc`，1,048,643 字节，SHA256 `4cb95482548c7fce23570a5bd6038b590c914dc7b687ee2efae36fdc2e594c52`。实际校验和、认证解密及 gzip 完整性检查通过。27 项既有迁移及校验和完全不变，未执行 DDL。

## 七月 PhotonPay 结果

先查到原任务失败、断点窗口 0 / 第 7 页 / 已成功 1,109 条，然后只创建一次 PhotonPay 月度刷新，没有刷新其他来源。新任务实际 `resumedFromPage=7`、`resumedFromWindow=0`；最终成功 5,807 条、失败 0 条，attemptCount=1，正式整月覆盖至 `2026-07-31T16:00:00Z`，完成后断点已清理。

本次生产运行未再次触发限流，因此生产验证证明了断点恢复和完整入账；1008 分类、有限退避、预算耗尽及页内重放已由隔离测试验证，不能声称在本次生产中观测到了退避。

七月所有卡来源账目合计从 1,568 到 5,841 条（包含 Airwallex，不是 PhotonPay 单源计数）；原有账目逐行摘要不变，重复外部交易号为 0。全部收入、人工调整、手工成本、费用、SUB 映射、员工统一 SUB、锁月、邮箱别名和卡排除配置摘要均保持不变。验收结束活动任务为 0，两个自动开关仍关闭。

## 保留的业务待办

- Influx：1 条 SUB 未映射，成功 0／失败 1，正式大盘显示“未入账”；需要业务确认映射，未猜填。
- Blitzads：1 条缺少 SUB，成功 4／失败 1，显示具体原因；需要联盟确认归属，未用时区调整掩盖归属问题。
- 仍有待统一 SUB ID 的员工行；本次未擅自更改业务标识。

这些问题使七月大盘仍显示数据不完整，不影响 PhotonPay 已完成的独立整月证据。

机器证据见 `monthly-finance-hotfix-production-evidence.json`；回退目标仍为之前的 `monthly-ea2b7f6a162f` 精确镜像，保留数据和分页状态，不做整库回退。
