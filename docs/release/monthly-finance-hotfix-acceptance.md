# 月度大盘热修与 CAKE 核对验收清单

当前状态：热修 `4328cad` 已于 2026-09-06 上线并完成七月 PhotonPay 单源验收，见 `monthly-finance-hotfix-production.md`。随后 CAKE `ea3b9f3` 经用户明确授权另行上线，见 `cake-monthly-review-production.md`，保留热修及全部旧财务数据。

## 已授权热修

- PhotonPay HTTP 200 / code 1008 分类为 RATE_LIMITED；普通业务错误保持不可重试，日志脱敏。
- 使用现有有限退避、尝试预算和执行期限；分页断点跨任务保留，重放不重复入账，记录首次恢复页。
- 大盘区分未入账、混合成功、等待重试、运行、终止失败；显示本次任务未映射与缺 SUB 数量。
- 原金额、上次真实成功时间和整月覆盖判定不变；只链接现有权限允许的页面。
- 隔离单元、真实 PostgreSQL、浏览器验证通过；使用确切提交构建 Node 22 API/Web。
- 发布前核查当前生产 monthly-ea2b7f6a162f、27 条迁移、活动任务及新备份。本热修不执行迁移。
- 发布后先查七月 PhotonPay 状态，仅必要时发起 PhotonPay 单源重试，记录断点、退避或最终结果、无重复及业务配置保留证据。
- 不调整 Influx 映射、Blitzads 缺 SUB、费用、人工调整、锁月；供应商继续限流必须如实记录。

## CAKE 新增候选（单独审核）

- 每个 CAKE 账号、月份默认显示“未核对GMT+8时区佣金”，独立于 API 成功及覆盖状态。
- 仅有效已确认人工调整可作为“已调整”依据；草稿/撤销无效，历史调整不补造记录。
- 支持明确“已核对，无需调整”及取消；保存操作者、时间、原生基准并审计，沿用调整权限和锁月约束。
- API 刷新同值保留核对；核对后原生金额变化显示待复核，不能继续显示无条件已核对。
- 跨月、跨账号隔离；取消最后调整且无独立确认恢复未核对；Everflow 和卡来源不受影响。
- 收入仍为原生佣金加已确认调整，不新增工资阻断或自动财务修正。
- 完成上述场景单元/数据库/实际页面验证，提交候选 SHA、迁移 SQL、测试记录给 PM；未审核不进入热修包。
