# 手动收入及 CAKE 批量发布

固定候选 e72202161c24624dc5b39a05031731a4899cf234，源归档 SHA256 4017614eaed45df37825cd2b80a7b89f8a068cf075d6d4922564b47fe890f8e2。默认 plan 不执行生产操作；必须先通过 PM 验收。

沿用现有网站、Docker Compose 和 root 环境配置。脚本核对当前真实 CAKE 镜像 ID、已应用 28 个迁移和校验和、无运行中同步、禁用自动调度。源包逐字节对比后构建，创建新加密备份并验证实际文件的校验和、认证解密和 gzip 完整性，然后停止旧应用，切换新镜像。没有执行迁移或数据库恢复命令。

切换前后比较全部收入、卡成本、手动成本、费用、映射、员工 SUB、锁月、CAKE 月度核对和同步任务哈希。正式接口只读核对 2026-07/08/09 收支、CAKE 状态及手动收入读取；不创建测试收入、不确认或停用调整、不触发供应商刷新。登录仅创建本次验收会话，浏览器使用加密传递的临时令牌并在验收结束撤销。

发布中失败回退固定的上一 CAKE API/Web 镜像，保留数据库。生产基线不符、账目发生变化、有在途同步、备份校验失败或目录已存在时停止，不能强行重跑。

sudo 和正式登录密码只能由用户在保持打开的交互窗口输入，不能通过聊天传递。运行参数 STAGE 必须为 /home/salaryops/manual-income-cake-batch-* 下绝对路径，APPROVED_CHECKS_SHA256 绑定 helpers 的 checks.sha256；密码不进入参数或输出日志。
